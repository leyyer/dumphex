Dump file to a big hex binary C array.
written in Haskell.

Install:
$ runhaskell Setup configure --prefix=$HOME --user
$ runhaskell Setup build
$ runhaskell Setup install

Usage:
$ dumphex [input] [output]
